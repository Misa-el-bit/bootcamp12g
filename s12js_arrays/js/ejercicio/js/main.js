/*  1___________
    crear una función que permita indicar la cantidad de koders a guardar, que pida tantos nombres de koders como se indique, y al final imprima el nombre del koder y su número asignado 
    Input:"Xoch","Oscar","Wendy"
    output: 
    Koder 1:"Xoch",
    Koder 2:"Oscar",
    Koder 3:"Wendy"
*/
/*
let numberOfKoders = prompt("Ingrese el numero de koders a guardar") //se solicita el numero de koders que se ingresaran y se guardan en una variable
function kodersList(numberOfKoders){
 let koders = [] //se declara una variable que contiene un array vacio, en el que se iran guardado los nombres de los koders
 let count = "" //se declara una variable que contiene un contador vacio, que se ira incrementando uando se recorra el nuevo array obtenido
 for (i=0; i< numberOfKoders; i++){ //se usa un for para recorrer el numero que indica cuantos nombres hay que ingresar
    koders.push(prompt("Ingrese el nombre del koder")) // se usa .push() para ir agregando al nuevo array cada uno de los nombres ingresados
 }
 for(let i=0; i<koders.length; i++){// se recorre el nuevo array obtenido 
    count ++ // conforme se recorre el array se suma en 1 al contador
    console.log(`Koder ${count}: ${koders[i]}`) //se imprime en consola el número del contador y el nombre correspondiente al index del array
 }
}

kodersList(numberOfKoders)
*/
/*
//crear una funcion que permita saber el nombre de algún koder basado en su número asignado
//imprimir la lista de koders en orden alfabetico - descendente
//crear una nueva lista que contenga los nombres de los koders con la sig, estructura:
// Koder 1: Israel Salinas Martinez (I.S.M)
*/

let koderList = ["Xochitl Moreno", "Alfredo Pizaña", "Wendy Ortega", "Oscar Hernández", "Héctor Alcaraz"]
/*
let numberAsigned = prompt("Ingresa el número del koder")
function koderName(numberAsigned){
    console.log(kodderList[numberAsigned])
}

koderName(numberAsigned)
*/
/*
function alfabeticOrder(koderList){
    console.log(koderList.sort())
}
alfabeticOrder(koderList)
*/


